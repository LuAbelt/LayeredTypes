fac x {
    let cond := (x == 0) in {
            if cond then {
                   1
            } else {
                    x * fac (x - 1)
            }
    }
}

print(fac(4))

test(42)